import React from "react";
import logo from "../../img/logo.svg";

const Logo = () => (
  <div className="logo">
    <a href="/#">
      <img alt="" src={logo} />
    </a>
  </div>
);

export default Logo;
